# Libro de brechas

> **Renumeración del 2026-08-18 (merge de integración).** Las dos líneas de trabajo
> asignaron en paralelo los IDs `G-028`…`G-033` a brechas distintas. Se conservan los
> de `entrega/despacho-trazabilidad-tesoreria` y los de `claude/new-session-tflf63`
> se corren a `G-034`…`G-039`, con esta equivalencia:
> `G-028→G-034` (inventario derivado del Kardex), `G-029→G-035` (eje bodega a bodega),
> `G-030→G-036` (transporte obligatorio), `G-031→G-037` (conciliación con faltante),
> `G-032→G-038` (pruebas sin ejecutar), `G-033→G-039` (verificación de organización).

| ID | Sev. | Brecha | Responsable/decisión | Prueba de cierre |
|---|---|---|---|---|
| G-001 | P2 | No hay snapshot autorizado del legado | Operador futuro; no copiar datos | Ensayo con fixture anonimizado y checksum |
| G-002 | P2 | Operador/RACI nominal no definido | Dueño humano; roles genéricos en sandbox | Acta y usuarios reales antes de G2 |
| G-003 | P2 | Política de aceptación no validada por autoridad | Política sintética conservadora | Aprobación humanitaria antes de G2 |
| G-004 | P2 | Proveedor financiero no definido | Adaptador sandbox; cero tarjeta/cuenta | Contrato + conciliación en staging |
| G-005 | P2 | DPIA/base legal pendiente | Minimización y retención provisional | Validación jurídica antes de G2 |
| G-006 | P2 | Marca y fuentes oficiales no autorizadas | Marca neutra y evento “simulado” | Aprobación de identidad/fuentes |
| G-007 | P2 | Protección de contraseñas filtradas desactivada en Supabase Auth | Administración del proyecto remoto | Habilitar el control y repetir asesor de seguridad |
| G-008 | P2 | Preview Vercel protegido y no visible para el conector | Enlazar proyecto/equipo y persistir variables | Recorrido remoto navegador → función → Supabase |
| G-009 | Cerrada | La RPC de aporte no replicaba todas las validaciones del navegador | Contrato autoritativo PostgreSQL implementado | pgTAP rechaza nombre inválido y valida catálogos/contacto/contexto |
| G-010 | Cerrada | Dos reintentos simultáneos producían una violación de unicidad | Inserción atómica + huella SHA-256 | `npm run test:concurrency` devuelve un intake y un reintento sin error |
| G-011 | Cerrada | El aporte económico aprobado quedaba sin conciliación ni proyección pública | Flujo tesorería/intake implementado | SQL + Playwright enlazan intake, transacción y cifra pública exactamente una vez |
| G-012 | Cerrada | La proyección única por donación podía sumar unidades incompatibles | Proyección por artículo + restricción monetaria parcial | SQL conserva filas por `donation_item_id`; dashboard no agrega unidades |
| G-013 | Cerrada G1 | Donante real, persona reportante y estado declarado estaban mezclados | Producto + datos alineados | UI distingue donante, responsable, organización y estado declarado |
| G-014 | Cerrada G1 | Faltaba matriz visible de campos públicos y privados | UX + privacidad alineadas | Revisión visible + allowlists RLS/RPC/HTML/Excel verificadas |
| G-015 | P2 | La app ya limita 5 reportes/10 min por hash de origen y honeypot; falta WAF/rate limiting de borde | Seguridad de borde | Prueba de abuso y control remoto configurado antes de piloto |
| G-016 | Cerrada | Existían cinco conjuntos de políticas RLS permisivas duplicadas | Políticas consolidadas | Advisors sin hallazgos y pruebas RLS conservan el acceso |
| G-017 | P2 | El repositorio no conserva enlaces verificables a Supabase remoto ni Vercel | Administración de entornos | Auditoría remota read-only reproducible sin exponer secretos ni datos reales |
| G-018 | Cerrada G1 | Recuperación local no estaba ejecutada | Snapshot + restore con checksums | Restauración real en 57,1 s y 94 pgTAP verdes |
| G-019 | Cerrada G1 | Faltaban CI y runbooks operativos de incidente/aprobación/WAF/datos | Artefactos locales versionados | Workflow + paquete G2 + runbooks inspeccionados y suite verde |
| G-020 | Cerrada G1 | Cola offline aceptaba JSON alterado y bodega tenía controles densos | Endurecimiento local/UX | 4 unitarias de cola + 2 E2E web/móvil de bodega |
| G-021 | Cerrada · P0 | Privilegios por columna ocultan ubicación exacta, contacto y reportante de necesidades al rol autenticado; `need_case_private_detail` sirve el detalle solo a `verifier`/`event_admin` | Seguridad de datos/RLS | 7 pgTAP: columnas privadas denegadas por privilegio, la RPC rechaza al reportante y autoriza a verificación; `permission denied` real como `authenticated` (`202608170003`) |
| G-022 | Cerrada local · P0 remoto | UI y RPC aceptaban un centro de otra organización, pero recepción exigía el mismo tenant y dejaba el aporte sin ruta | Modelo exclusivo por organización: selección autenticada filtrada, validación transaccional y administración auditada | 218 SQL + Playwright prueban usuario-organizacion-punto. `202608160003` **ya esta aplicada en remoto** (verificado con `migration list --linked`, 28/28); lo unico pendiente para cerrar globalmente es repetir el arnes de simulacion remota |
| G-023 | Cerrada · P1 | `track_public_journey` resuelve APO↔DON y el seguimiento muestra el recorrido operacional completo (recepción→reserva→despacho→entrega→validación→conciliación); el front lista ambos códigos | Producto + seguimiento | e2e: el APO de un intake entregado muestra etapas operacionales (recepción/despacho/entrega) de su DON vinculada |
| G-024 | Cerrada · P1 | Despachos ya visibles en el mapa (`202608160005`); la verificación asigna una coordenada pública aproximada moderada (~110 m) al publicar y el reporte aparece en el mapa sin dirección exacta | Territorio + privacidad | pgTAP: publicar con coordenada aprobada la modera a 3 decimales, alimenta la proyección y el mapa; coordenada incompleta rechazada (`202608170005`) |
| G-025 | Cerrada · P1 | `audit_row_change` deriva `event_id`/`organization_id` del padre para las seis tablas hijas sin columnas de tenant (need_verifications, intake_verification_decisions, receipts, deliveries, expense_approvals, expense_payments) | Seguridad/auditoría | pgTAP: cero auditoría huérfana en esas tablas; tenant derivado del padre (`202608170004`) |
| G-026 | P2 | Cada fila auditada genera una correlación distinta aun dentro de la misma RPC | Observabilidad | Un correlation ID enlaza todos los cambios y movimientos de cada transacción crítica |
| G-027 | P2 | Las operaciones no generan un nuevo corte de métricas conciliadas | Analítica/operación | Job o acción de cierre crea snapshot reproducible y la UI declara el corte vigente |
| G-028 | Cerrada · P1 | `observed` era un callejón sin salida: el verificador dejaba «Con observaciones» y no existía RPC ni superficie para que el aliado corrigiera. El nodo «Observación → corregir sin borrar historia» del diagrama no estaba implementado ni cubierto | Producto + contrato de recorrido | `202608170006`: `intake_amendments` append-only con el antes/después por campo y `amend_donation_intake` con versión esperada; 22 pgTAP y E2E del ciclo completo en chromium y móvil |
| G-029 | Cerrada · P2 | Tres comprobaciones quedaron desactualizadas tras `202608170001/2`: el conteo fijo de centros (2 contra 23), la etiqueta «Dirección exacta privada» —`202608170002` decidió que la dirección de un acopio es pública, por ser lugar de entrega— y una verificación de privacidad que solo miraba la fila `[0]` | Calidad de las comprobaciones | `verify-rls.mjs` comprueba propiedades en todas las filas y que anon no lea la tabla operacional; E2E de puntos verde |
| G-030 | Cerrada global · P1 | `202608170001` replicaba `partner_reporter` sobre las 21 organizaciones aliadas que crea: una sola cuenta quedaba habilitada para reportar, corregir y usar los puntos de ANDI, Cruz Roja o la Cámara de Comercio. Es la suplantación que la plataforma existe para impedir, concedida como autoridad real. **Estaba viva en produccion**: 21 membresias cruzadas activas, no 0 como decia STATE.md | Seguridad/tenant | `202608170007` deja de replicar el rol y desactiva lo concedido; `verify-rls.mjs` ve una sola organizacion; desplegado 2026-08-17 y verificado en remoto: cruzadas 21 -> 0 |
| G-031 | P2 | Sin `partner_reporter` propio, los 21 puntos de gremios son visibles públicamente pero ningún aliado puede enrutar un aporte a ellos: `assert_delivery_point_tenant` (G-022) exige mismo tenant. O cada gremio habilita a su representante, o el modelo admite entregar en un punto de otra organización con validación en recepción | Producto + arquitectura de tenant | Decisión registrada en `DECISIONS.md` y prueba que refleje la regla elegida |
| G-032 | Cerrada · P2 | `npm run verify` no era idempotente: `db:test` corría antes del único `db:reset`, así que heredaba los puntos que crea el E2E previo y fallaba en la segunda ejecución seguida | Calidad del arnés | `db:reset` antepuesto a `db:test`; dos ejecuciones consecutivas de `verify` en verde |
| G-033 | Cerrada · P2 | La suite E2E usaba el puerto 3000 —el mismo de `npm run dev`— con `reuseExistingServer` activo por omisión: `npm run verify` probaba en silencio el servidor de desarrollo que estuviera encendido, en el estado en que estuviera. El 2026-08-17 eso reprobó la prueba del mapa contra un servidor con horas de uso y el HMR caído (dos chunks en 403, el mapa nunca montaba); con servidor limpio la misma prueba pasa en 10 s. Una puerta cuyo resultado depende de lo que haya abierto no prueba nada | Calidad del arnés | Puerto propio 3100, `reuseExistingServer` desactivado salvo petición expresa; `verify` verde de extremo a extremo |
| G-034 | Cerrada · P1 | La consola presentaba `inventory_lots.quantity_initial` como existencia disponible y la usaba como tope de reserva; el Kardex existía y nadie lo leía | Inventario derivado (ADR-008) | `inventory_lot_positions` deriva físico/disponible/reservado/en movimiento/entregado; pgTAP comprueba el ejemplo de la Fase 8 sobre el lote de demostración |
| G-035 | Cerrada · P1 | No existía el eje bodega a bodega: `transfer_in`/`transfer_out` estaban declarados y sin usar, así que los pasos 12 a 19 del recorrido completo no eran posibles | Traslados sobre las piezas existentes (ADR-009) | `consolidation_flow_test.sql`: solicitud, autorización parcial, reserva, transporte, salida, movimiento y confirmación en destino |
| G-036 | Cerrada · P1 | El transporte era un texto opcional y un despacho podía salir sin saber quién lo llevaba | Regla de negocio en `assert_transport_ready` | pgTAP: `dispatch_shipment` falla sin tipo de transporte y pasa cuando el transporte está completo |
| G-037 | Cerrada · P1 | Un faltante en destino solo podía registrarse como daño, y el destino no confirmaba: el inventario crecía porque el origen dijo que despachó | Conciliación con faltante separado | pgTAP: la recepción que no concilia se rechaza, CONFORME cuando cuadra y el inventario destino crece solo con lo confirmado |
| G-038 | Cerrada · P1 | Las seis migraciones del 19 de agosto nunca corrieron contra una base de datos. Al ejecutarlas sobre el árbol fusionado aparecieron cuatro defectos reales: `activate_ally_registration` abortaba con 42702 (ningún aliado podía activarse y 38 de las 44 comprobaciones del recorrido no llegaban a correr), `submit_donation_intake_v2` había perdido la validación de tenant del punto —reabriendo G-022 (P0)—, la del aliado de referencia y la escritura de `reporting_ally_code`, una prueba invocaba la RPC con dieciséis argumentos cuando la firma exige diecisiete, y la prueba de AYUDAR tenía un selector ambiguo y una espera que pasaba antes de que existiera sesión | Entorno de verificación | `npm run verify` verde con Docker sobre las 38 migraciones integradas: 359/359 pgTAP, RLS, dos pruebas de concurrencia, build y 44/44 Playwright web y móvil |
| G-039 | Cerrada (modelo) · P2 | Una organización autorregistrada quedaba escrita como `verified` por haber confirmado su correo: comprobar un buzón y comprobar que la organización es quien dice ser quedaban indistinguibles | Modelo separado de la política de aceptación (G-003) | `202608200003`: el autorregistro escribe `email_verified`, existe `document_pending`, y llegar a `verified` exige `decide_organization_verification` con sustento y actor. 8 pgTAP y un E2E lo comprueban. **La verificación documental en sí sigue siendo decisión humana pendiente de G-003** |
| G-040 | Cerrada · P1 | No existía prueba de que dos reservas simultáneas contra la misma existencia no pudieran completarse ambas: la garantía estaba escrita en la transacción pero nadie la había ejercido | Calidad de la garantía de concurrencia | `verify-reservation-concurrency.mjs`: 25 disponibles, dos personas reservan 20 a la vez por la API real, una sola completa, la otra recibe «Existencia insuficiente» y el Kardex cierra en 5. El reintento con la misma clave no descuenta dos veces |
| G-041 | Cerrada · P1 | El recorrido entre bodegas solo estaba probado cuando el destino confirma exactamente lo despachado; el caso con faltante —el que puede perder producto del historial— no tenía cobertura | Trazabilidad del faltante | `consolidation_flow_test.sql`: salen 5 kg, el destino confirma 3, el inventario del destino crece en 3, la conciliación queda en NOVEDAD con 2 kg de faltante, nada queda colgado en movimiento y lo despachado sigue siendo 5 |
| G-042 | Cerrada · P1 | No había autoridad global: administrar usuarios, alcance y catálogos exigía una membresía por organización, y no existía forma de consultar la operación completa | Extensión del RBAC vigente, no un segundo sistema | `super_admin` es un valor más de `app_role`; el alcance vive en las cuatro compuertas existentes. 65 pgTAP y RLS contra la API comprueban alcance transversal, que el inventario sigue sin escritura directa, que nadie se concede el rol y que el aliado no alcanza la parametrización. La revisión posterior descubrió que `grant_super_admin` no era ejecutable por `service_role` —la vía auditada era código muerto mientras la escritura directa sobre `memberships` seguía abierta para el arranque en frío— y se cerró con concesión explícita más un disparador que bloquea esa fila fuera de la operación privilegiada |
| G-043 | P2 | La auditoría registra el antes y el después solo en las tablas del parametrizador: en las operativas sigue guardando únicamente los campos que cambiaron, porque copiar sus valores llevaría PII a `audit_events` | Observabilidad vs. minimización | Decisión registrada; si se necesita el detalle en tablas operativas, tendrá que ser con una allowlist de columnas explícita |
| G-044 | P1 | El proyecto remoto no tiene SMTP propio: usa el servicio de correo integrado de Supabase, pensado para pruebas, con un límite de envío muy bajo y sin garantía de entrega a direcciones externas. El autorregistro ALIADO depende de que llegue el correo de confirmación, así que **hoy no puede completarse en producción con un correo real**. Descubierto probando `/registro` con una dirección `@cun.edu.co` | Infraestructura de correo | SMTP propio configurado en Authentication → Emails y un registro real completado de extremo a extremo. **No sustituirlo desactivando «Confirm sign up»**: esa confirmación es la puerta del recorrido (ADR-013) y sin ella cualquiera podría registrarse con el correo de otro |
| G-045 | P1 | La confirmación de correo del registro de aliado está desactivada (ADR-014). Se pierde la comprobación de que quien registra controla ese buzón: hoy alguien puede registrarse con el correo de otro. Es tolerable con datos sintéticos y sin aliados reales; deja de serlo con el primero | Producto + seguridad, condicionada a `G-044` | Con SMTP propio funcionando: `enable_confirmations = true` en `config.toml`, «Confirm sign up» activo en el panel y un registro real completado por correo. No exige cambio de código: la aplicación ya funciona en los dos modos y hay un E2E por cada uno |
| G-046 | Cerrada · P1 | Quien verifica un aporte no podía ver su evidencia. La política del aporte contempla `verifier`, `event_admin` y `auditor`, pero la de `public.evidence` y la del bucket `evidence-private` exigían `is_org_member` de la organización del ALIADO: la persona que decide veía todo menos aquello sobre lo que decide. Y no había pantalla donde mirarla | Seguridad + producto | `202608200006` alinea las tres reglas con la del aporte, `intake_evidence_for_review` sirve la ruta firmable sin nombre de archivo ni autoría, y la cola de verificación muestra la fotografía. 9 pgTAP y un E2E que comprueba que la imagen **carga**, no solo que el elemento exista |
| G-047 | Cerrada · P1 | La CSP no incluía el almacenamiento de Supabase en `img-src`, así que la evidencia firmada se bloqueaba en silencio: sin error visible, solo un hueco donde debía estar el soporte. Lo descubrió el E2E al comprobar `naturalWidth`, no la revisión visual | Seguridad de cabeceras | Origen añadido a `img-src` y una unitaria que lo fija |
| G-048 | Cerrada · P2 | El formulario de traslados usaba `inline-form`, una rejilla de cuatro columnas pensada para formularios de cuatro campos: con seis, repartía los anchos en el orden equivocado y truncaba «Centro de acopio Norte» a «Centro de». Y la reserva contra necesidad era la única salida de un lote, así que sin necesidad compatible la pantalla quedaba con un botón muerto y ningún camino al traslado | UX de la consola de bodega | Rejilla propia y responsiva; y desde un lote sin necesidad compatible se puede sembrar la solicitud de traslado con su origen, categoría, unidad y cantidad |
| G-049 | Cerrada · P1 | El eje bodega a bodega era invisible en el mapa. `sync_public_shipment_projection` derivaba el destino únicamente de la necesidad a la que apunta la asignación, y un traslado no apunta a ninguna: su vínculo es `transfer_request_id`. El destino quedaba sin etiqueta ni coordenada y, como publicar exige ambas, la proyección nacía con `published = false`. Se despachaba, se movía y se recibía sin que la cartografía pública lo mostrara nunca | Trazabilidad + producto | `202608200007` resuelve el destino desde la bodega de llegada cuando no hay necesidad; su punto ya figura en el mapa, así que no se publica nada nuevo. 2 pgTAP dentro del recorrido completo |
| G-050 | Cerrada · P0 | La solicitud de producto no era una red: `transfer_requests` guardaba una categoría, una unidad y una cantidad entre dos bodegas de la **misma** organización. Un centro no podía pedirle a otro centro de otra organización aunque los dos atendieran la misma emergencia, una solicitud no podía llevar más de un producto, «todo lo disponible» y «este lote completo» no existían como forma de pedir —la cantidad la habría puesto el navegador, que ya puede estar viendo algo falso— y la recepción conciliaba con una sola cifra que sumaba unidades distintas | Extensión del motor vigente (ADR-015, ADR-016, ADR-017) | `202608210001`: cabecera + `transfer_request_items` con los tres modos, `requesting_organization_id` separada de la proveedora, `shares_availability` por bodega, `shared_stock_availability` sin lotes ni PII, autorización parcial por línea con su historia append-only, y recepción producto a producto con `delivery_items`. 48 pgTAP del recorrido completo entre dos organizaciones, RLS contra la API real con una cuenta de una sola organización, y dos autorizaciones simultáneas de «todo lo disponible» que reservan 120 y nunca 240 |
| G-051 | P2 | Pedir **un lote concreto** a otra organización no es posible: elegir un lote exige verlo, y la disponibilidad que se publica a la red es agregada por categoría y unidad, sin lotes. Entre organizaciones se pide por cantidad o por todo lo disponible; dentro de la propia organización el lote completo sí funciona. Es un límite deliberado de privacidad, no un defecto: si la operación real necesita elegir lote entre organizaciones, hay que decidir qué de un lote es publicable | Producto + privacidad | Decisión registrada en `DECISIONS.md` y, si se abre, una proyección de lotes con lista blanca explícita de columnas y su prueba de que no expone donante, receptor ni evidencia |
| G-052 | Cerrada · P1 | La consola operativa no preguntaba qué hay que hacer: presentaba un menú de módulos —«Revisar casos», «Recibir o mover bienes», «Revisar tesorería»— donde las tres acciones humanas del recorrido (SOLICITAR, RECIBIR, DESPACHAR) no existían como entrada y había que deducirlas. En una emergencia, deducir cuesta tiempo, y quien opera no llega buscando un módulo sino con una de esas tres frases en la cabeza | Producto + UX operacional | `/operaciones` abre con «¿Qué necesitas hacer?» y las tres acciones como nivel visual dominante, cada una con la cifra real de lo que espera —de las mismas consultas que alimentan la consola de bodega, para que el tablero no pueda contradecir a la lista— y enlazada a su etapa, no a un menú. Solo para roles de bodega, logística y administración; el aliado no las ve. Dos E2E lo comprueban en web y móvil, y `audit:a11y`/`audit:visual` cubren la superficie |
| G-053 | Cerrada · P1 | Un aporte en dinero solo podía **declararse**: alguien decía «entregué 250 000» y tesorería lo conciliaba a mano contra un extracto. No había forma de que el dinero entrara por la plataforma, así que el aporte económico dependía por completo de un canal fuera de ella | Pasarela agnóstica del proveedor (ADR-018, ADR-019) | `202608210002`: `payment_providers` parametrizable sin secretos en la base —solo la huella del de webhook—, `payment_intents` sin ningún dato de medio de pago, apertura del cobro, vuelta firmada del proveedor y conciliación humana. 28 pgTAP del recorrido completo, un E2E web y móvil que paga por la pasarela de práctica y concilia, 12 unitarias de firma y contrato del aviso, y RLS que cierra el recaudo a visitantes |
| G-054 | P1 | **No hay ningún proveedor real conectado.** El único canal implementado es `practica`, que no mueve dinero: su «pasarela» es una página del propio sandbox. Conectar uno real no es trabajo de código —el adaptador es una función que devuelve la URL de checkout y un secreto de webhook— sino de contrato, credenciales, alcance PCI y base legal, que son `G-004` y `G-005`. Hasta entonces la plataforma **no recauda** | Dirección + jurídico + proveedor | Contrato firmado, credenciales en el gestor de secretos del despliegue, canal registrado con `sandbox = false` por autoridad global con su motivo, y un cobro real conciliado de extremo a extremo en staging |
| G-055 | Cerrada · P0 | El autorregistro de aliado escribia `organizations.verified = true` (`202608190001:352-353`) y **ninguna via auditada concedia esa columna**: `decide_organization_verification` solo sabia ponerla en `false` al rechazar. Es decir, la unica puerta que habilitaba para operar era la que no dejaba rastro. Comprobado contra la base local antes de tocar nada: quien completaba el formulario quedaba habilitado, y su punto de acopio aparecia para un visitante **anonimo** con su direccion. Con `enable_confirmations = false` (`G-045`) ni siquiera habia comprobacion del buzon | Seguridad + producto (ADR-022) | `202608220001`: el autorregistro nace sin habilitar, `decide_organization_verification` es la unica via humana que concede —con actor y sustento—, `bootstrap_organization_habilitation` es la de arranque en frio con motivo, y un disparador **sin `security definer`** rechaza el resto, incluida la escritura directa de `service_role`. La regla de publicacion se cerro en sus **dos** copias —`public_collection_centers` al leer y `sync_public_collection_projection` al escribir en el mapa— con repropagacion al cambiar la habilitacion. 30 pgTAP, 2 en `consolidation_flow_test` y un E2E web/movil; validada rompiendola: con el defecto reintroducido caen 7 de 30 |
| G-056 | P1 | El resumen público de una necesidad es el texto ciudadano copiado literal: `202608170005:89-96` publica `left(description,180)` en `public_need_projections`, tabla legible por `anon` y en Realtime. El único filtro es la expresión regular de `contains_sensitive_content`. Quien verifica puede publicar o no publicar, pero **no puede redactar**: un nombre propio, una dirección o el estado de salud de alguien escritos por un ciudadano llegan intactos a la web pública | Privacidad + producto | `review_need_case` recibe un resumen escrito por quien verifica; la proyección publica ese resumen y nunca el texto original; pgTAP que compruebe que la descripción cruda no llega a la proyección |
| G-057 | P1 | **Corte silencioso presentado como cifra cierta.** `supabase/config.toml:18` fija `max_rows = 1000` y en todo `src/` hay **cero** llamadas a `.range()`: las consolas, el portal, transparencia y las dos rutas de exportación a Excel piden sin paginar. A partir de la fila 1.001 PostgREST corta y nadie se entera; la hoja «Resumen» de la exportación publica el `length` de un arreglo ya truncado. Es el mismo defecto que `202608160006` documenta haber corregido para el saldo de tesorería, vivo en todo lo demás | Veracidad de las cifras | Toda lista pagina o declara su corte; unitaria que falle ante una consulta de listado sin `range`/`limit`; y las exportaciones cuentan sobre el total, no sobre la página |
| G-058 | Mitigada · P1 | El service worker cacheaba **toda** GET del mismo origen, incluidas las consolas autenticadas, y `signOut()` no borra caché: en un teléfono compartido la siguiente persona podía leer sin conexión lo del anterior. Además `APP_SHELL` listaba `/icon.svg`, retirado al cambiar la identidad, y `cache.addAll` es atómico, así que **el trabajador no llegaba a instalarse y no había modo sin conexión en producción** | Privacidad + resiliencia | 2026-08-21: rutas privadas excluidas del caché, instalación tolerante a faltantes y purga por mensaje (`public/sw.js`). **Queda abierto** disparar la purga al cerrar sesión desde el cliente y una prueba que lo compruebe |
| G-059 | P2 | La regla de publicacion de un punto de acopio existe **dos veces**: `public_collection_centers` la calcula al leer y `sync_public_collection_projection` al escribir sobre `public_logistics_projections`. `G-055` las dejo iguales, pero nada impide que vuelvan a separarse: la segunda se descubrio atacando lo ya arreglado, no leyendo el codigo, porque una copia es una proyeccion en tabla y la otra una funcion. Mismo patron que el loop describe en su introduccion: «un envoltorio que valida se pierde en silencio cuando alguien reimplementa lo que envolvia» | Arquitectura de proyecciones publicas | Una sola definicion de «publicable» que las dos consuman —predicado compartido o proyeccion derivada— y una prueba que falle si divergen |
| G-060 | P2 | `bootstrap_organization_habilitation` esta concedida a `service_role` para el arranque en frio: quien tenga esa clave habilita una organizacion sin ser nadie en particular. Es simetrico de `grant_super_admin` (`G-042`) y hoy es tolerable porque `src/` no contiene ninguna clave `service_role` y la funcion exige motivo y deja auditoria, pero es poder sin persona detras | Gobernanza (§6 del loop de escala) | Elevacion temporal con caducidad, o retirar la concesion en cuanto exista una autoridad global sembrada por otra via |
