# Matriz de trazabilidad inicial

| Requisito | Diseño/implementación | Prueba | Estado |
|---|---|---|---|
| Ingreso no se publica automáticamente | `donation_intakes` separado de `public_donation_projections` | E2E H | Probado |
| Necesidad basada en hechos | RPC sin urgencia/rescate predeterminados | E2E C + Playwright | Probado |
| Inventario exactamente una vez | RPC idempotente + `stock_movements` | E2E A/D/G | Probado |
| Dinero conciliado y segregado | libro append-only + aprobación por actor distinto | E2E B + UI | Probado |
| Evidencia/ubicación privada | Storage privado, RLS y vistas explícitas | RLS + E2E C/H + simulación remota IDOR | **Bloqueado G2**: `anon` denegado, pero `partner_reporter` alcanzó ubicación/contacto de necesidad (`G-021`) |
| Lote restringido no sale | hold/recall validado en asignación | E2E F | Probado |
| Migración no verifica | staging en cuarentena + rollback por lote | E2E E | Probado con fixture |
| Mapa público con precisión protegida | MapLibre + cartografía vectorial real OpenFreeMap/OSM + RPC `public_need_map` + PostGIS; solo usa coordenadas aproximadas y conserva lista accesible | SQL/RLS + E2E web/móvil + inspección visual | Probado en sandbox |
| Mapa real sin WebGL | Fallback Leaflet con teselas OpenStreetMap, necesidades, centros y líneas origen-destino; conserva filtros y detalle semántico | Inspección visual en navegador integrado sin WebGL + E2E | Probado en sandbox |
| Centros de acopio sin exponer direcciones | RPC `public_collection_centers`, columnas públicas aproximadas y tarjetas/mapa; dirección exacta queda fuera de la proyección | SQL/RLS + E2E web/móvil | Probado en sandbox |
| Reglas por punto y compatibilidad real | `item_acceptance_rules.location_id` permite excepción por punto y conserva regla organizacional predeterminada; `/operaciones/centros` parametriza zona, dirección privada, instrucciones, coordenadas, disponibilidad, frío y categorías mediante RPC idempotente/auditada | 152 pgTAP/E2E SQL + Playwright web/móvil + rechazo explícito de tenant cruzado | Probado local · `G-022` cerrado localmente; migración y repetición remota pendientes |
| Selección de punto aislada por organización | `organization_delivery_points` exige membresía, devuelve solo puntos activos del tenant y hereda reglas generales cuando no existe excepción; `submit_donation_intake_v2` vuelve a validar evento-organización-punto | Pruebas de aliado, admin, anon, cruce de tenant, reglas heredadas y flujo completo | Probado local; remoto conserva el contrato anterior hasta autorización |
| Catálogos de la matriz sin duplicación | Ocho catálogos PostgreSQL versionados alimentan tipos de donante, sectores, 13 categorías, cuatro estados, cinco departamentos de cobertura, geografía nacional, unidades y 22 aliados de referencia; la UI recibe el contrato desde un componente servidor | Parser unitario + 152 pruebas SQL + migraciones locales hasta `202608160003` | Probado local; remoto conserva `202608160001` hasta autorización de migración |
| Aliado derivado y verificado | La organización autorizada procede de `memberships` para evento/rol vigente y `organizations.verified/status`; el selector autenticado de aliado es solo referencia interna y no concede identidad, membresía ni marca pública | RLS + validación RPC + pruebas negativas de permisos/valor inventado | Probado local; altas institucionales reales requieren verificación y autorización |
| Estado declarado no acredita entrega | `comprometida`, `en_gestion`, `en_transito` y `entregada` se conservan como declaración; el intake sigue `pending_verification` y no crea `donations` antes de aprobación | pgTAP específico + E2E monetario web/móvil | Probado local |
| Consolas acotadas a su evento | `/operaciones`, bodega y tesorería filtran roles y datos por `event_id`; las tablas sin evento propio se acotan por su padre con join interno | Revisión de cada consulta + recorrido sobre un evento distinto al del seed | Probado local; con un solo evento el defecto era invisible |
| Saldo financiero no inferido | `treasury_balance` suma el libro completo del evento y solo de las organizaciones del usuario; el indicador declara cuántos movimientos incluye y se oculta a roles sin tesorería | pgTAP contra la suma directa del libro + denegación al aliado reportante | Probado local |
| Justificación de gasto auditable | `approve_expense` exige una nota escrita por quien decide y `expense_decisions` la devuelve acotada; la tabla conserva RLS sin política de lectura directa | pgTAP de ausencia de política y de contrato de la función + comprobación en pantalla | Probado local |
| Cadena de trazabilidad de punta a punta | `track_public_journey` devuelve los hitos ocurridos con fecha para NEC, APO y DON; el código del aporte atraviesa su donación operacional hasta la validación de la entrega o la conciliación económica, ordenado por etapa del proceso | pgTAP de formato, fechas y no filtración + 14 comprobaciones del recorrido | Probado local; el detalle sale con etiquetas fijas y sin transportador, dirección ni contacto |
| Punto con propósito declarado | `accepts_donations` y `dispatches_shipments` con restricción que impide un punto sin propósito; el acopio gobierna mapa público, proyección logística y selección del aliado, y el despacho gobierna el origen permitido de una salida | pgTAP de propósito y proyección + denegación de origen inválido en el recorrido | Probado local |
| Despacho con origen comprobable | `shipments.origin_location_id` con validación de tenant, punto activo, propósito y destino público sin contenido sensible; la consola pide origen, destino y transportador en lugar de asumirlos | pgTAP de origen cruzado + idempotencia y destino sensible en el recorrido | Probado local |
| Entorno reproducible desde cero | `bootstrap:environment` declara evento, organizaciones, cuentas por rol, centros de acopio y fondos en un JSON fuera del repositorio; confirma el destino, genera contraseñas, verifica roles y aislamiento entre organizaciones, y es reejecutable sin reescribir lo que ya coincide | `db reset --no-seed` + provisión + `verify:environment-flow` con 17 comprobaciones del recorrido | Probado local sobre evento y organizaciones distintos al seed; el destino remoto sigue requiriendo autorización |
| Centros de acopio parametrizados en la entrega | La provisión crea cada punto con `manage_delivery_point` bajo sesión `event_admin`: reglas de aceptación versionadas, dirección exacta privada, coordenada aproximada y auditoría append-only, idéntico a `/operaciones/centros` | pgTAP de privilegios + verificación de visibilidad propia y denegación cruzada entre organizaciones | Probado local |
| Aporte guiado y comprobable | Flujo adaptativo de cuatro pasos en especie y tres en dinero, con campos obligatorios a la vista y contexto opcional plegado; centro preferido, contexto privado, aliado de referencia, hasta tres fotos privadas, organización derivada, donante/responsable separados, estado declarado y ticket QR `APO-*` sin PII | SQL transaccional + E2E web/móvil con carga real a Storage | Probado local en especie y dinero; el contrato del RPC no cambió |
| Solicitudes comprensibles | Revisión previa define qué se enviará y qué no acredita; centro operativo separa necesidad/aporte y muestra resumen natural, hechos clave y siguiente control sin códigos de catálogo ni término “intake” | Playwright de confirmación y cola operativa en escritorio/móvil | Probado local |
| Evidencia fotográfica del intake | Reserva transaccional, ruta generada por servidor, JPG/PNG ≤5 MB, SHA-256, máximo tres, bucket privado, vínculo auditado y confirmación contra objeto existente; `pending` no equivale a evidencia validada | pgTAP de contrato/privacidad/objeto + carga Playwright web/móvil | Probado local; escaneo antimalware y política de consentimiento bloquean uso real |
| Excel público seguro | Ruta dinámica con hojas de resumen, necesidades, aportes publicados, centros y metodología; tipos nativos, fórmulas reproducibles y neutralización de fórmulas inyectadas | Unitarias Excel + descarga y apertura real en E2E | Probado en sandbox |
| Excel operativo autorizado | Ruta autenticada; consultas sujetas a RLS; excluye nombre, correo, teléfono, observaciones y contactos internos | 401 anónimo + descarga autenticada y apertura de seis hojas | Probado en sandbox |
| Dashboard sin métricas engañosas | Cobertura por necesidad y dashboard de aportes; cuenta proyecciones por categoría, conserva cantidades con su unidad y separa COP conciliados | SQL + E2E + Excel + simulación de corte | **Parcial**: filas dinámicas correctas; snapshots no se actualizan tras la operación (`G-027`) |
| Marcas institucionales controladas | Identidad neutral en sandbox; logos y franjas territoriales requieren autorización expresa de publicación | Nota visible y revisión de límites | Bloqueado fuera del sandbox |
| Entrada ciudadana comprensible | Categorías visuales con controles radio semánticos; se preservan moderación, campos privados y confirmación de buena fe | 4 E2E web/móvil | Probado en sandbox |
| Centro operativo orientado a tareas | Lanzador condicionado por membresía para verificar, recibir, consultar y conciliar | Auth/RLS + E2E de roles | Probado en sandbox |
| Actualización territorial en vivo | `public_need_projections` y `public_logistics_projections` en `supabase_realtime`; centros y despachos se refrescan mediante RPC sujetas a RLS | SQL de publicación/proyección + RLS + conexión E2E | Probado en sandbox |
| Despachos georreferenciados sin GPS público | Trigger deriva origen del centro y destino de la necesidad publicada; expone código, estado y coordenadas aproximadas, nunca custodia/transportador/ruta exacta | E2E SQL + fixture + despacho remoto real | **Parcial**: fixture verde; reporte ciudadano remoto no crea coordenadas y el despacho no se publica (`G-024`) |
| Seguimiento comprensible sin rastrear personas | línea de hitos derivada de `track_public_code`, sin ubicación ni evidencia privada | E2E web/móvil + RLS + QR remoto hasta entrega | **Parcial**: `DON-*` llega a validado, pero el QR `APO-*` queda aprobado (`G-023`) |
| Accesibilidad y responsive | filtros con `aria-pressed`, gráficos con nombre accesible, tablas equivalentes, campos requeridos, errores vivos, stepper y control de desbordamiento | 28 pruebas Playwright en Chromium y móvil | Probado básico |
| Validación autoritativa del aporte | La única RPC ejecutable por la aplicación valida contrato, versiones de catálogo, aliado de referencia, organización derivada, punto activo del mismo tenant y coherencia por tipo | Pruebas negativas directas + 152 pruebas SQL | Probado local · G-009/G-022 cerradas localmente |
| Idempotencia concurrente | Inserción `ON CONFLICT`, recuperación de resultado y huella SHA-256 del payload | Dos conexiones simultáneas + prueba de payload distinto | Probado · G-010 cerrada |
| Aporte monetario conciliado | Intake aprobado → cola segura → fondo verificado → transacción/recibo → proyección pública | pgTAP + Playwright web/móvil | Probado · G-011 cerrada |
| Proyección por artículo | Cada artículo en especie conserva categoría, unidad y cantidad verificadas sin agregación cruzada | Restricción única `donation_item_id` + recorrido de entrega | Probado · G-012 cerrada |
| Clasificación público/privado completa | Confirmación divide campos publicables tras verificación y campos privados; superficies públicas usan allowlist | UI web/móvil + RLS + Excel | Probado G1 · G-014 cerrada |
| Entrada ciudadana antiabuso local | Honeypot + cuota transaccional 5/10 min por hash de origen/evento; firma anterior revocada y contadores sin privilegios públicos | pgTAP de cuota, privacidad y privilegios + E2E | Probado local; WAF remoto pendiente |
| Observabilidad mínima sin PII | logs JSON por operación, hook de errores servidor, request ID, Server-Timing y salud no cacheable | E2E de `/api/health` y cabeceras | Probado local |
| Recuperación sintética | snapshot de esquema/datos, manifiesto SHA-256, reset, truncado controlado, restore y pgTAP | restauración real en 57,1 s + 94 pruebas | Probado local |
| Cola offline mínima segura | Solo UUID/cantidades/condición; esquema estricto, TTL 72 h, máximo 50, deduplicación e idempotencia | 4 unitarias con cargas alteradas, vencidas y exceso | Probado local; política de dispositivo bloquea G2 |
| Operación de bodega amigable | Etapas, búsqueda, campos etiquetados, tarjetas de lote y filtro categoría/unidad | 2 Playwright web/móvil + contratos PostgreSQL | Probado local |
| Integración continua | GitHub Actions levanta Supabase local, crea env mínima, ejecuta `npm run verify` y conserva Playwright si falla | Ejecución `31958813505` sobre `main` | Probado remoto |
| Sandbox remoto reproducible | Vercel con variables Production/Preview, Supabase con 15 migraciones y bootstrap mínimo sintético sin ejecutar `seed.sql` remoto | Build Vercel `dpl_6isVPmpj83EKqKFX6DPRpjUpLVqM`; salud 200; reporte activo; dos centros; CSP; smoke público | Probado remoto 2026-08-16; limitado a datos sintéticos |
| Auth remoto cerrado y no enumerado | Altas globales deshabilitadas; el login no publica correos; cinco cuentas sintéticas tienen contraseñas temporales únicas y 12 membresías; secreto administrativo fuera del frontend/Vercel | Auth Admin de provisión + cinco logins con clave pública + Playwright web/móvil de no enumeración | Probado remoto; HIBP y rotación operativa pendientes |
| Simulación integral remota por actor | UI pública/aliado y RPC con sesiones admin, bodega, solicitante y aprobador; especie y dinero hasta estados finales, reintentos, superficies públicas y auditoría | `SIM-20260816195948-9f2d8d2b`: 38/38 controles, 46 eventos, Excel 14.394 bytes | Funcionalmente probada con datos sintéticos; ocho hallazgos `G-021` a `G-027` bloquean G2 |
| Gobierno y respuesta | Runbooks de incidentes, datos, WAF y paquete de aprobación enlazan bloqueos con responsable/evidencia | revisión documental + preflight bloqueado | Preparado; aprobación humana pendiente |
